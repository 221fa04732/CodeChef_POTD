#include <bits/stdc++.h>
using namespace std;
void solve(int a,int b,int c)
{
    if(a>b && a>c)
        cout<<"Setter"<<"\n";
    else if(b>a && b>c)
        cout<<"Tester"<<"\n";
    else 
        cout<<"Editorialist"<<"\n";
}
int main() 
{
	int t;
	cin>>t;
	while(t--)
	{
	    int a,b,c;
	    cin>>a>>b>>c;
	    solve(a,b,c);
	}
}
